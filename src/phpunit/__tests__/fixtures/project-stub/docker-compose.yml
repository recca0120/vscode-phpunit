version: '3.8'
services:
    php:
      build:
          dockerfile: Dockerfile
      container_name: php
      tty: true
      volumes:
          - .:/var/www/html
      networks:
          - app-network

networks:
  app-network:
    driver: "bridge"
